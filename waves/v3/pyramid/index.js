import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const pyramid = {
  lib: 'wave',
  id: 'bz6Z',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: 'pyramid',
  i18n,
  config,
  makeAdapter,
}

export default pyramid
