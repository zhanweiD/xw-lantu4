import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const renju = {
  lib: 'wave',
  id: 'JbNC',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: 'renju',
  i18n,
  config,
  makeAdapter,
}

export default renju
