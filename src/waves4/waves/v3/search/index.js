import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const search = {
  lib: 'wave',
  id: 'ZpyZ',
  version: '1.0.0',
  completed: true,
  description: '低优先级',
  icon: 'search',
  i18n,
  config,
  makeAdapter,
}

export default search
