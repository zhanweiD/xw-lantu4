import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const bullet = {
  lib: 'wave',
  id: '6GBE',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: 'bullet',
  i18n,
  config,
  makeAdapter,
}

export default bullet
