import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'
const uiTable = {
  lib: 'wave',
  id: '3xiP1',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: 'table',
  i18n,
  config,
  makeAdapter,
}

export default uiTable
