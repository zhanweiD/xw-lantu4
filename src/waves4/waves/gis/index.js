import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const gis = {
  lib: 'wave',
  id: '8FRm',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: 'gis',
  i18n,
  config,
  makeAdapter,
}

export default gis
