import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const stackLineColumn = {
  lib: 'wave',
  id: 'lcsk',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: 'stack-line-column',
  i18n,
  config,
  makeAdapter,
}

export default stackLineColumn
