import {config} from "./config"
import Adapter from "./adapter"
import i18n from "./i18n"

const groupColumn = {
  lib: "wave",
  id: "cngp",
  version: "1.0.0",
  completed: true,
  description: "",
  icon: "group-column",
  i18n,
  config,
  Adapter
}

export default groupColumn
