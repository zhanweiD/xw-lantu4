// // const requireAll = (requireContext) => requireContext.keys().map(requireContext)

// const svgs = import.meta.glob("./*.svg")
// console.log(svgs)
// // const icons = require.context('../waves4/icons', false, /\.svg$/)
// // requireAll(icons)
// // requireAll(svgs)
// for (const path in svgs) {
//   svgs[path]().then((mod) => {
//     console.log(path, mod)
//   })
// }
