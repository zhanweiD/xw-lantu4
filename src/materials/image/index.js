// 内置的背景图
import {config} from './config'
import makeAdapter from './adapter'
import i18n from './i18n'

const image = {
  lib: 'wave',
  id: 'image',
  version: '1.0.0',
  completed: true,
  description: '',
  icon: '',
  i18n,
  config,
  makeAdapter,
}

export default image
